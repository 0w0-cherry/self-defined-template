#include<assert.h>
char* my_strcat(char* dest, const char* src)
{
    assert(dest && src);
    char* ret = dest;
    //1.找到目标空间中的 \0
    while (dest != '\0')
    {
        dest++;
    }
    //2.拷贝数据
    while (*dest++ = *src++)
    {
        ;
    }
    return ret;
}
